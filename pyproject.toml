[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "astronomy-ml"
version = "0.1.0"
description = "A reproducible machine learning pipeline for astronomy image data."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]